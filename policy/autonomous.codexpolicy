prefix_rule(
    pattern = ["~/.config/codex/skills/autonomous-skill/scripts/run-session.sh"],
    decision = "allow",
    match = [["~/.config/codex/skills/autonomous-skill/scripts/run-session.sh", "--list"]],
)

prefix_rule(
    pattern = ["codex", "exec"],
    decision = "allow",
    match = [["codex", "exec", "prompt"]],
)