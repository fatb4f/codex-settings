Tool-crafting execution scripts live here.

Suggested responsibilities:
- Validate `tool.manifest.json` against the tool manifest schema.
- Validate `dag.json` against the DAG schema.
- Optionally generate `dag.mmd` from `dag.json` (no-hand-edit rule).
